#IDLE
#dir()描述一个对象的所有内置方法
#list2=list1 相当于指针指向同一对象，list3=list1[:]是对list1的拷贝
#r'' 原始字符串，将''内的字符串全部转义，但结尾的\不能转义
#append,extend,insert
#remove,del,pop
#2*（2）=64,2*(2,)=(2,2)，元祖不可修改，只能通过拼接重新赋值
#字符串可以像元祖一样操作
#format方法:'{0},{1},{2}'.format('x','y','z')等价于'{a},{b},{c}'.format(a='x',b='y',c='z') 输出为'x,y,z'
#字符串格式化 : '{0:1f}{1}'.format(27.658,'GB')输出'27.7GB','%c %c' % (97,98) 输出 'a b'+
#函数 def test(*a,b=1,c),a为收集参数，参数个数无限多；b为默认参数，默认赋值为1；c为普通参数，不传入则会报错